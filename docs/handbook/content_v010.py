# -*- coding: utf-8 -*-
"""《Divana 项目里的 Agent 工程》增补章：v0.8 ~ v0.10（评测与测量）。

单独一个文件，是因为 build_handbook.py 要把这两章**插在面试速查之前**，
而不是接在最后。改文字不碰排版，这条规矩照旧。

文内引号统一用中文引号（“ ”），省得和 Python 字符串的引号打架。
"""

CHAPTERS = [
    # ============================================================ 第 6 章
    ("h1", "第 6 章　评测：怎么知道她变差了"),
    ("lead", "这一章回答：一个会自己决定调什么工具的 agent，你怎么知道这次改动是变好还是变坏。"),

    ("p", "v0.10 之前，改 prompt 是<b>盲改</b>：你觉得这次说得更清楚了，但你不知道它修好了"
          "一件事的同时有没有弄坏三件事。更麻烦的是，agent 的输出是不确定的——"
          "同一句话问两遍都可能不一样，靠“我觉得这次答得不错”根本没法迭代。"),
    ("p", "所以这一章的核心不是“怎么给分”，而是<b>怎么把“你要的行为”写成可自动判的句子</b>。"
          "这件事做对了，后面全是工程。"),

    ("h2", "6.1　第一课：评测会逼你说清“我到底要什么”"),
    ("p", "真实翻车：有一条用例是“搜一个根本不存在的东西”，一开始她答得含糊，"
          "于是往 prompt 里加了三行——搜不到时优先使用“没有找到相关内容”"
          "“没有搜索到相关内容”“搜索结果为空”。分数立刻上去了。"),
    ("note", "但那是<b>教她背答案</b>：把“如实说”这个行为，降级成了“说特定几句话”。"
             "换个模型、换批用例就不成立；更糟的是她可能不分真假都说这几句。"
             "这在评测里叫 teaching to the test，面试官会专门盯这一点。"),
    ("p", "正确的做法反过来：<b>prompt 只写原则，把断言放宽</b>。后来那三行被删掉，"
          "断言从“词表”换成了“结构”（见 6.2）。"),

    ("h2", "6.2　判据怎么写：结构优先于措辞"),
    ("p", "同一个判据“她有没有如实说没搜到”，三种写法，可靠性差一个量级："),
    ("table", {
        "head": ["写法", "长什么样", "什么时候会误判"],
        "widths": [0.18, 0.48, 0.34],
        "rows": [
            ["词表", "回答里出现 没搜到 / 没有结果 之一",
             "她说“没有一条跟它有关的东西”——意思全对，词表一个都没命中"],
            ["正则（好）", "否定词 + 结果词出现在同一行",
             "基本不会；真要挑刺，是“有否定词但结论错”"],
            ["模型裁判", "让另一个模型判“她是否如实”",
             "贵、有噪声；判“事实对不对”最不可靠"],
        ],
    }),
    ("p", "词表还有更隐蔽的副作用：<b>加词等于放宽范围</b>。当时为了少挂一条，"
          "往词表里加了“输入错误”——等于把“她反过来指责用户打错字”也放行成了通过。"
          "字符串匹配没有语义，加词就是加漏洞。"),
    ("p", "但正则也不是万能的：<b>窗口会写窄</b>。有一条用例她写的是"
          "“要不要把这套「embedding 是什么」存成一篇笔记”，而正则只允许"
          "“要不要”和“存”之间隔 14 个字符——实际隔了 18 个，于是判定她没问。"),
    ("note", "这一册最该记住的一句：<b>断言是尺子，尺子会撒谎。</b>"
             "稳定地挂（每次都挂）通常说明尺子或规则有问题，而不是模型退化；"
             "随机的挂才该去量化（--attempts 3）。"),

    ("h2", "6.3　三层断言：轨迹 → 文本 → 产物"),
    ("p", "判“她做了什么”分成三层，从最稳到最不稳："),
    ("table", {
        "head": ["层", "判什么", "字段", "为什么需要它"],
        "widths": [0.12, 0.16, 0.34, 0.38],
        "rows": [
            ["轨迹", "她做了什么", "expect_tools / forbid_tools / max_llm_calls",
             "最稳、最便宜，先判这个"],
            ["文本", "她说了什么", "expect_pattern（正则判结构）",
             "措辞会变，所以判结构不判用词"],
            ["产物", "她改对了没", "expect_files（读临时 vault 里的文件）",
             "调了工具不等于做对了"],
        ],
    }),
    ("p", "第三层是 v0.10.3 加的，起因很实在：有一条用例她调了 update_plan、工具也返回了"
          "“已更新”，但<b>文件里里程碑还是没打勾</b>。如果只断言“她调了工具”，"
          "这种丢数据会一直安静地发生（真因见 7.1）。"),

    ("h2", "6.4　隔离靠结构，不靠小心"),
    ("p", "每条用例都跑在自己的临时目录里：画像、计划、笔记、会话库全是新建的，跑完就删。"
          "代码上就一个口子——装配上下文时把根目录传进去："),
    ("code", "build_context(settings, root=tmp / \"vault\")"),
    ("p", "<b>评测会真的让 agent 写文件</b>，所以“不碰真实数据”必须是结构上保证的，"
          "而不是靠“评测的时候我会小心”。这条规矩在写第一个用例之前就立下了。"),

    ("h2", "6.5　报告是对比，不是分数"),
    ("p", "孤立的“通过 14/18”没有意义：模型会更新、温度会抖、你的用例也会改。"
          "有意义的是<b>和上一次比出来的差值</b>，所以报告的第一段永远是这种："),
    ("code", "[回归] plan-update    <- 原来能过，现在挂了（模型调用不超过 4 次）\n"
             "[修好] no-unprompted-note\n"
             "[新增] note-offer（未过）"),
    ("p", "两个容易踩的口径："),
    ("ul", [
        "<b>读数只用“用例通过率”</b>：--attempts 3 下 18 条 = 54 次*运行*，"
        "但规则是“三次全过才算这条通过”。报“54 次里挂 2 次”是把抖动洗掉了，"
        "写进简历会被追问这个数怎么算的。",
        "<b>baseline 要记 commit</b>，而且工作区有未提交改动时要标成 +脏——"
        "否则你会把“这一版的分数”记到一个并不包含你那份 prompt 的提交上，回头复现不出来。",
    ]),

    ("h2", "6.6　失败要能看轨迹，否则没法改"),
    ("p", "报告只告诉你“哪条断言挂了”，看不出她<b>绕到哪去了</b>。所以失败时会写一份"
          "完整轨迹到 evals/trajectory/：这一轮你说什么 → 每次工具调用的名字 / 参数 / 返回 → "
          "她每一段话 → 跑完之后临时 vault 里的文件。"),
    ("p", "这份东西第一次立功，是发现“四个工具全挂在同一次模型调用上”——"
          "一眼就知道不是她的问题，是工具归因写错了（见 7.4）。"),

    ("h2", "6.7　成本要能拆开，而且行为归行为、成本归成本"),
    ("p", "“超过 token 上限”这种失败，光看总数等于没看。所以跑完会打一张分布表，"
          "轨迹里还有逐次明细：每次模型调用的 input / output / 其中推理、"
          "每个工具返回的体量、最后那段回答的体量，并标出最贵的那一次。"),
    ("p", "项目实测出来的几个数（18 条全量）："),
    ("ul", [
        "<b>input 占 83% ~ 92%</b>：钱几乎全花在“每次调用都要重发”的上下文上。",
        "<b>推理（思考）占 output 的 36% ~ 75%</b>：她“说出来的话”只占总成本的 0.8%——"
        "回答长短根本不是问题。",
        "<b>固定底噪实测 4248 token / 次</b>：人格 3163 字符 + 工具 schema + 用户消息。",
        "所以成本 ≈ 调用次数 ×（底噪 + 累积历史 + 推理），"
        "<b>每多绕一轮工具约 +5 ~ 6k token</b>。",
    ]),
    ("p", "这直接改变了断言的写法。原来只用 max_tokens，可它实际在卡“调用次数”，"
          "而调用次数会在两种<b>合法行为</b>之间波动：有一条用例，她“不顺手记画像”是 1 次调用，"
          "“顺手记画像”是 2 次——上限正好卡在两者中间，同一条用例于是反复过 / 挂。"),
    ("note", "解法是把两件事拆开：<b>max_llm_calls 卡行为</b>（调用次数，稳定、可解释），"
             "<b>max_tokens 只当防爆表护栏</b>（数值 = 调用上限 × 单次实测成本 × 1.2）。"
             "它挂了的意思是“比最坏的合法路径还多花 20% 以上”，那才值得查。"),

    ("h2", "6.8　故障注入：怎么测“工具坏了”"),
    ("p", "工具的<b>失败路径</b>是 agent 最容易露馅的地方，而它平时测不到（key 配好了、网也通）。"
          "做法是用例直接覆盖配置："),
    ("code", "given:\n  settings:\n    search_api_key: \"\"    # 搜索工具会正常失败，然后看她怎么反应"),
    ("p", "这条用例来自一次真实失败：那天 key 没配好，她凭<b>通用经验</b>编了一串排查步骤，"
          "还建议“去代码里搜 DIVANA_SEARCH_API_KEY”——可她当时并没读过这份代码。"
          "把“工具失败”当成“我能自己推理出原因”，是 agent 很典型的坏行为。"),

    ("h2", "6.9　用例从哪来：回归优先"),
    ("p", "别为了评测去造对话。最好的用例是<b>某天她真的答错了，把当时那句话抄下来</b>——"
          "那是回归用例，比手写合成的有价值得多。"),
    ("p", "项目里至少三条就是这么来的，来源是恢复出来的历史会话：版本号该联网查、"
          "工具挂了要如实说、讲完概念要问一句要不要存笔记。"
          "顺着这条线，评测集会长成“你在乎的行为清单”，而不是“凑够二十条”。"),

    ("h2", "6.10　面试怎么讲评测这件事"),
    ("p", "一句话版本：<b>我给自己的 agent 建了 18 条轨迹级评测：三层断言、baseline 回归对比、"
          "失败自动出轨迹、成本能拆到每次调用。它抓出过 4 个我自己写的 bug。</b>"),
    ("table", {
        "head": ["面试官会问", "怎么答"],
        "widths": [0.38, 0.62],
        "rows": [
            ["你怎么判好坏？",
             "三层：轨迹（做了什么）/ 文本（说了什么，用正则判结构）/ 产物（文件改对没）。"
             "文风类留给将来的模型裁判。"],
            ["为什么不用模型裁判？",
             "贵、有噪声、判事实最不可靠。要上它的前提是先有人标 20~30 条参考答案量一致率，"
             "否则只是换了个没校准的尺子。"],
            ["怎么防止过拟合评测集？",
             "不把断言的具体措辞写进 prompt；断言只写行为原则；回归用例来自真实失败。"],
            ["跑一遍多少钱？",
             "18 条 = 33 次模型调用 = 168k token，一次几毛钱。单条平均 9.3k。"],
            ["你怎么知道是模型退化，还是评测坏了？",
             "先看稳定挂还是随机挂，再去看轨迹和明细。这半个月四次翻案，"
             "四次都是评测口径的问题——见第 7 章。"],
        ],
    }),
    ("pagebreak", ""),

    # ============================================================ 第 7 章
    ("h1", "第 7 章　测量：六个骗过我的坑"),
    ("lead", "这一章是这本册子里最值钱的部分。六个“分数变差”，最后都证明是测量错，"
             "不是她变差。学会这套，你在任何 agent 团队都能立刻干活。"),

    ("h2", "7.1　一次响应里的两个工具是并发的"),
    ("p", "现象：她一次要求改计划的两节（两个 update_plan 在<b>同一条响应</b>里），"
          "两个工具都返回“已更新计划的…”，可文件里只留了一个改动——里程碑还是没打勾。"),
    ("p", "（顺带说明为什么会有两个调用：v0.11 之前 update_plan 一次只能改一节，"
          "所以改两节只能调两次。改成一次多节之后，这个场景本身就是历史了。）"),
    ("p", "排查链：轨迹里看到参数明明是 - [x]，文件里却是 - [ ]；于是写了个 300 轮的并发写实验，"
          "结果丢更新、抛 WinError 32，还读到过 UnicodeDecodeError（文件被写成半截）。"),
    ("p", "根因有三层，缺一层都不行："),
    ("ol", [
        "<b>SDK 把同步工具丢进线程池</b>（agents/tool.py 里是 asyncio.to_thread），"
        "所以同一条响应里的多个工具是<b>真并发</b>，不是顺序执行。",
        "<b>write_section 是“读整份 → 改一节 → 写整份”</b>。两个线程各自基于同一份旧快照"
        "写回去，后写的把先写的整个盖掉——教科书级的 lost update。",
        "<b>原子写的临时文件名是固定的</b>（xxx.md.tmp）。两个写者抢同一个临时文件："
        "一个刚换走、另一个 os.replace 时文件已经不在。改成唯一名字之后又暴露出第四层："
        "Windows 上两个线程同时替换<b>同一个目标文件</b>，os.replace 会直接 WinError 5"
        "拒绝访问（目标被占用），所以还要加退避重试。",
    ]),
    ("note", "修法：唯一临时文件名（mkstemp）+ 按路径写锁（<b>读和写都在锁里</b>，"
             "只锁写不锁读照样丢）+ 替换退避重试（顺带覆盖 Obsidian / 编辑器 / 杀毒软件"
             "短暂占用文件的真实场景）。修完 300 轮 0 异常、0 半截文件。"),
    ("p", "<b>面试价值最高的一个坑</b>：它证明“我调了工具、工具也返回成功”不等于"
          "“数据写对了”。被追问“你的多文件写入并发安全吗”时，这条能从头讲到尾。"),

    ("h2", "7.2　只读 final_output：“她其实答了”被判成“没答”"),
    ("p", "现象：一条用例挂“没有回答用户的问题”，只看报告你会以为她只顾着记账。"
          "但她的原话是：“要不要我把<b>刚才这套</b>（embedding 是什么 + 它和向量数据库的分工 + "
          "那个练习）整理成一篇笔记？”——她自己认为已经讲过了。"),
    ("p", "证据在 token 明细里：<b>第 1 次调用 output 2669</b>（其中推理 1969，"
          "非推理约 700 ≈ 一次完整讲解），<b>第 2 次调用 output 只有 79</b>。"
          "也就是说她在“要调工具”的那次响应里就把话讲完了。"),
    ("p", "根因：RunResult.final_output 取的是<b>最后一次模型调用</b>的文本"
          "（run.py 里写死 final_output = turn_result.next_step.output）。"
          "模型“先说话 → 再调工具 → 最后收尾”时，前面那段正文根本不在 final_output 里，"
          "而评测的正文只读了 final_output。"),
    ("code", "for item in result.new_items:\n"
             "    if isinstance(item, MessageOutputItem):\n"
             "        said.append(ItemHelpers.text_message_output(item))   # 按顺序全收\n"
             "text = \"\\n\\n\".join(said) or str(result.final_output or \"\")"),
    ("p", "<b>这条坑骗过我一次</b>：上一轮我判定“她把解释写进画像、一个字没讲给用户”，"
          "并据此改了 prompt 和工具返回文案——那份证据是错的。所以这条要写在最显眼的地方："
          "<b>凡是只看最后一句话的评测或日志，都会丢内容。</b>"),

    ("h2", "7.3　捕获过宽的 except 加重试：测试把自己搞崩"),
    ("p", "现象：网页层测试偶发失败；两条命令并行跑时会成片失败。"),
    ("p", "排查：先怀疑端口竞争（改成自己占住监听 socket 再交给服务器）→ 再怀疑机器"
          "（另写一个裸 http.server 压测，900 个请求零失败）→ 最后给每个请求打时间线，"
          "才看见真相：挂的全是“故意期待 400 / 404”的用例，每个都重试了 6 次、卡 1.6 秒。"),
    ("p", "根因一句话：<b>urllib.error.HTTPError 是 URLError 的子类</b>，"
          "而代码里写的是 except (ConnectionError, URLError)——把正常的 400 / 404 当成了"
          "传输故障去重试。单轮连接数因此翻三倍，反而把真正的连接重置制造了出来。"),
    ("ul", [
        "修法：子类先拦，HTTPError 直接抛出去。",
        "教训：<b>重试的捕获范围要比“看起来合理”更窄</b>。判断标准不是“异常类名像不像"
        "网络错”，而是“传输没走通”还是“服务端正常答复了”。",
        "顺带一个工程习惯：偶发的测试比没有测试更糟——它让你开始怀疑自己的代码。"
        "所以修完要能说清修的是环境还是代码，并留下可观测的计数"
        "（这次是每次跑完打一行 transport-retry 次数）。",
    ]),

    ("h2", "7.4　记账口径：同一个数字，四种错法"),
    ("p", "这一节的四条全是“报告自己不可信”，而且都让我一度得出错误结论："),
    ("table", {
        "head": ["错的记法", "后果", "正确做法"],
        "widths": [0.32, 0.38, 0.3],
        "rows": [
            ["多轮用例每轮覆盖成本统计", "只算了最后一轮的钱，而 max_tokens 又是拿它比的",
             "按轮累计"],
            ["工具归因取事件流里最近一次调用",
             "raw_responses 是整轮一次性灌进列表的，取最后一个永远是最后一次——"
             "4 个工具全挂到最后一次调用上",
             "看每次调用自己的输出里有没有 function_call"],
            ["--attempts 3 时断言取失败那次、成本取最后一次",
             "同一份轨迹里出现“成本 5335”和“实际 13468”打架",
             "整份结果都取失败的那一次"],
            ["临时文件名固定，两个写者共用", "丢更新 + 半截文件",
             "唯一临时名 + 写锁 + 重试"],
        ],
    }),
    ("note", "<b>一份自相矛盾的报告，比没有报告更糟</b>——它会让你理直气壮地错怪别人。"
             "所以报告要能自己和自己对上：断言、成本、轨迹必须来自同一次运行。"),

    ("h2", "7.5　改完必须验证：三次“改到了别的地方”"),
    ("ol", [
        "<b>单行补丁打错位置</b>：文件里 max_tokens: 16000 出现多次，改 note-search 时"
        "改到了 plan-read 头上。靠“按用例 id 逐条对照 18 条”才发现。",
        "<b>IDE 解冲突选“两边都保留”</b>：把我已经删掉的一段有 bug 的代码又带了回来，"
        "而且测试全过——因为测试没覆盖那几行。",
        "<b>rg 静默返回空</b>：它默认跳过隐藏目录<b>并且遵守 .gitignore</b>，而 .venv/ 两样都占，"
        "所以前面几次搜 SDK 源码都以为“没有这个东西”，白猜了半天。",
    ]),
    ("p", "三次的共同点：<b>工具会骗你，而且不报错</b>。所以改完一定要用另一种方式验证一遍——"
          "对照表、diff、把生成物重新读出来。这也是为什么这本册子里每次修 bug 都跟着一条测试。"),

    ("h2", "7.6　六个坑的共同点，以及面试怎么讲"),
    ("p", "共同点有三条：① 都不报错；② 看起来都像“她的问题”；"
          "③ 只有把原始数据摊开（轨迹、明细、文件内容）才看得出来。"),
    ("note", "一句话方法论：<b>先问“我这次的测量可信吗”，再问“她是不是变差了”。</b>"
             "顺序反了，你会花几天去调一个根本不存在的模型问题。"),
    ("table", {
        "head": ["追问", "答法"],
        "widths": [0.34, 0.66],
        "rows": [
            ["评测里最难的是什么？",
             "不是写用例，是保证“你测的就是你以为在测的”。我踩过四次：成本只算了最后一轮、"
             "工具归因挂错调用、并发写丢数据、正文只读到最后一次调用。"],
            ["你怎么发现这类问题的？",
             "① 稳定挂还是随机挂先分类；② 失败时写完整轨迹，把参数、返回、文件都摊开；"
             "③ 和 baseline 比变化；④ 能复现的就写并发 / 压力实验去复现。"],
            ["这些经历让你养成了什么习惯？",
             "三个：报告必须自洽；改完必须用另一种方式验证；断言不许和接口、输入、"
             "prompt 规则打架——打架的时候输的永远是断言。"],
            ["如果让你给团队搭评测？",
             "先把“她做了什么”判掉（便宜、稳定）；文本用结构正则；产物读文件。"
             "模型裁判排最后，而且要配人标参考答案。"],
        ],
    }),
    ("pagebreak", ""),

    # ============================================================ 第 8 章
    ("h1", "第 8 章　评测驱动开发：一套能搬走的做法"),
    ("lead", "这一章回答：换个项目，这套评测该怎么搭、怎么用、什么时候该停。"),

    ("h2", "8.1　先说收益：评测到底改进了什么"),
    ("p", "这半个月里，18 条用例一共逼出 6 个改动。把它们列出来，因为这张表才是"
          "“评测有没有用”的答案："),
    ("table", {
        "head": ["评测报的现象", "真相", "改了什么"],
        "widths": [0.26, 0.38, 0.36],
        "rows": [
            ["plan-update 的里程碑没勾上",
             "并行工具调用并发写，丢了更新——工具还返回了成功",
             "唯一临时文件名 + 按路径写锁 + 替换退避重试"],
            ["“她一次调用调了 4 个工具”",
             "工具归因写错了（全挂到最后一次调用上）",
             "改成看每次调用自己的输出"],
            ["“她没回答我的问题”",
             "只读了最后一次调用的文本——她把话讲在调工具那一段里了",
             "按顺序收这一轮的所有文本"],
            ["“她太贵了”", "多轮用例每轮覆盖统计，只算了最后一轮", "改成累计"],
            ["“她绕圈：改计划调了两次工具”",
             "接口一次只能改一节——是设计逼她这么做的",
             "update_plan 改成一次能传多节"],
            ["三条用例反复挂",
             "断言和规则自己打架（断言 vs 接口、vs 输入、vs prompt）",
             "改断言、删掉 prompt 里矛盾的那句"],
        ],
    }),
    ("note", "看最后两列：<b>六个改动里，没有一个是“模型不行”</b>——全是测量、接口或规则的问题。"
             "这句话就是评测最大的价值：它把“感觉它不对”变成“知道哪里不对”，"
             "而且大多数时候不对的是我们自己。"),
    ("p", "除了修 bug，评测还长出四样<b>基础设施</b>，它们让下一次查得动：失败自动出轨迹、"
          "成本能拆到每一次调用、故障注入（把 key 置空测失败路径）、"
          "以及行为与成本分开的断言（max_llm_calls 管绕圈，max_tokens 只防爆表）。"),

    ("h2", "8.2　最小可用版：三步"),
    ("ol", [
        "<b>写 10 ~ 15 条“你在乎的行为”</b>，每条一句话请求。别为评测造对话——"
        "某天它真的答错了，把那句话抄下来就是最好的一条。",
        "<b>每条只判“它做了什么”</b>：调了哪些工具、几次、有没有超上限。"
        "这一层零成本、零噪声，先把能自动判的判掉。",
        "<b>存 baseline，之后只看差值</b>。报告第一屏永远是 [回归] / [修好] / [新增]，"
        "而不是“通过 14/18”这种孤立分数。",
    ]),
    ("p", "别一开始就追求“判得准”。先能跑起来、能对比，再谈判据的精度——"
          "本项目的第三层断言（读产物文件）是跑到 v0.10.3 才加的。"),

    ("h2", "8.3　判据怎么升级：三层"),
    ("table", {
        "head": ["层", "判什么", "代价", "什么时候上"],
        "widths": [0.12, 0.28, 0.26, 0.34],
        "rows": [
            ["轨迹", "调了什么工具、几次", "几乎为零", "第一天就上"],
            ["文本", "说了什么（正则判结构）", "要写正则，会误判", "有“该不该说某句话”的要求时"],
            ["产物", "文件 / 数据被改对了吗", "要读临时目录", "有写操作的能力一上线就加"],
            ["模型裁判", "质量、准确性", "贵、有噪声、要先校准", "确定性层再也挑不出毛病之后"],
        ],
    }),
    ("note", "上模型裁判的前提不是“有钱”，而是<b>先有人标参考答案</b>："
             "拿 20 ~ 30 条人工判断，量裁判与人的一致率。没有这一步，它只是个没校准的尺子。"),

    ("h2", "8.4　加完一个能力，先问“怎么判它做对了”"),
    ("p", "这条规矩能省掉很多返工。例子：给项目加上“记学习画像”这个能力以后，"
          "只断言“它调了 update_learner_profile”是不够的——它完全可能把内容写进错的那一节。"
          "所以要读文件，看那节里到底有没有那句结论。"),
    ("p", "同一件事的另一面：<b>调了工具不等于做对了</b>。这条在本项目里是被一次真实事故"
          "打脸的——并发写丢了数据，工具却返回“已更新”（见 7.1）。"),

    ("h2", "8.5　失败时先问两个问题，顺序不能反"),
    ("ol", [
        "<b>这次测量可信吗？</b> 稳定挂还是随机挂？轨迹里它到底做了什么？成本明细里"
        "钱花在哪一次？有没有可能是我读错了（只读到一半的文本、只算了最后一轮）？",
        "<b>测量没问题的话，才是它的锅吗？</b> 还是接口逼它这么做、两条规则互相矛盾、"
        "或者我给它的输入本来就不清楚？",
    ]),
    ("note", "先怀疑尺子，再怀疑人。<b>顺序反了，你会花几天去调一个根本不存在的模型问题。</b>"),

    ("h2", "8.6　一个案例的五次翻转"),
    ("p", "如果这一章只记一件事，记这个：<b>plan-update</b> 这条用例前后翻了五次，"
          "每一次都是“我以为 X，数据说 Y”。"),
    ("table", {
        "head": ["第几次", "我以为", "数据说"],
        "widths": [0.14, 0.4, 0.46],
        "rows": [
            ["1", "模型调用次数超了，它在转圈", "接口一次只能改一节，它别无选择"],
            ["2", "它改计划时顺手改画像，越权", "prompt 自己两条规则打架，按哪条都错"],
            ["3", "它没把里程碑勾上，能力不够", "并发写丢了更新，工具还返回了成功"],
            ["4", "成本超上限，它太啰嗦", "token 上限卡在两种合法行为中间"],
            ["5", "精简工具说明能省成本", "省了 306 字符，但单次差值的多半是波动"],
        ],
    }),
    ("p", "五次里没有一次是“模型不行”。<b>这不是巧合——这就是 agent 工程的日常。</b>"),

    ("h2", "8.7　成本要和行为分开量"),
    ("ul", [
        "<b>行为指标</b>（模型调用次数、工具次数）稳定、可解释 → 拿它当断言。"
        "本项目用 max_llm_calls 卡“绕了几圈”。",
        "<b>成本指标</b>（token）会被推理开销和输入长度带得上下飘，同一条用例能差一倍 "
        "→ 只当“防爆表”的护栏，不当行为断言。",
        "<b>固定开销才是大头</b>：每次调用都要重发人格 prompt 和工具定义。本项目实测"
        "人格 3185 字符 + 工具 4674 字符 ≈ 4300 token/次，第一次调用的 input 实测 4248——"
        "乘 33 次调用，占整份评测集成本的 83%。",
    ]),
    ("p", "所以杠杆是有顺序的：<b>少调一次模型</b>（本项目 4248/次，比整个人格 prompt 还贵）"
          "大于按需拼 prompt，大于抠字。"),

    ("h2", "8.8　知道什么时候停手"),
    ("p", "继续抠字最多再省 80 token/次，可<b>验证一次改动要跑一遍全量评测</b>"
          "（本项目 168k token）。投入产出是负的时候，正确动作是停手，"
          "并且把这个判断也写下来——省得下次又从头算一遍。"),
    ("note", "<b>成本优化看的是规模，不是“还能不能再省一点”。</b>"
             "一天问几十句的个人项目，省 80 token/次毫无意义；等调用量真上来了，"
             "再回头动“调用次数”和“按需拼 prompt”这两条大杠杆。"),

    ("h2", "8.9　搬到别的项目：一张清单"),
    ("ol", [
        "有没有一份“我在乎的行为”清单？10 ~ 15 条起步。",
        "每条判的是轨迹还是文风？文风的现在就不要自动判。",
        "跑评测会碰真实数据吗？必须靠结构隔离（临时目录），不靠小心。",
        "有没有 baseline？报告里有没有“和上次的差值”？",
        "失败时能不能看到“它到底做了什么”？（轨迹 / 日志 / 中间产物）",
        "成本能不能拆到每一次模型调用？",
        "行为断言和成本断言分开了吗？",
        "断言有没有和接口、输入、prompt 规则打架？（打架时输的永远是断言）",
        "工具与环境的失败路径测过吗？（把 key 置空、让工具返回 404）",
        "知道这笔优化的验证成本吗？它决定你该不该动手。",
    ]),
    ("pagebreak", ""),
]

