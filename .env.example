# 复制成 .env 后填写。.env 已被 .gitignore 排除，不会被提交。

# 你的 API key（必填）
DIVANA_API_KEY=sk-...

# 接口地址：默认 DeepSeek 官方。以后换别的 OpenAI 兼容网关就改这里
DIVANA_BASE_URL=https://api.deepseek.com

# 模型名：当前该接口可选 deepseek-v4-pro / deepseek-flash
DIVANA_MODEL=deepseek-v4-pro